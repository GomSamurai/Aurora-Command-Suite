<Window x:Class="AuroraDesignSuite.Views.NewIndustrialOrderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nueva Orden de Construcción Industrial" Height="380" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource BgDarkBrush}" Foreground="{DynamicResource TextPrimaryBrush}">
    
    <Window.Resources>
        <SolidColorBrush x:Key="CardBgBrush" Color="#161C2A"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2A344A"/>
        <SolidColorBrush x:Key="AccentCyanBrush" Color="#00F0FF"/>
        <SolidColorBrush x:Key="AccentAmberBrush" Color="#FFB400"/>
    </Window.Resources>

    <Border Padding="20" BorderBrush="{DynamicResource AccentCyanBrush}" BorderThickness="1" CornerRadius="8" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="🚀 ASIGNAR PROYECTO INDUSTRIAL PLANETARIO" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="15"/>
                <TextBlock Text="Registra una nueva orden de construcción en los centros industriales del imperio." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Form Body -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="Instalación / Estructura a Construir:" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>
                <ComboBox x:Name="CmbTargetInstallation" Height="32" Margin="0,0,0,15" Padding="8,4" Background="{DynamicResource CardBgBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}"/>

                <TextBlock Text="Cantidad de Unidades a Fabricar:" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="TxtQuantity" Text="10.0" Height="32" Padding="8,6" Background="{DynamicResource CardBgBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}" FontSize="13"/>
                <TextBlock Text="💡 Nota: Las fábricas planetarias asignarán su capacidad BP automáticamente según la prioridad del imperio." Foreground="{DynamicResource AccentCyanBrush}" FontSize="10" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Cancelar" Click="BtnCancel_Click" Width="100" Height="32" Margin="0,0,10,0" Background="{DynamicResource CardHeaderBrush}" Foreground="{DynamicResource TextSecondaryBrush}" BorderBrush="{DynamicResource BorderBrush}"/>
                <Button Content="🚀 Registrar Orden" Click="BtnConfirm_Click" Width="140" Height="32" Background="{DynamicResource CardHeaderBrush}" Foreground="{DynamicResource AccentGreenBrush}" BorderBrush="{DynamicResource AccentGreenBrush}" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
