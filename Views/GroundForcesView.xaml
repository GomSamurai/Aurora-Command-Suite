<UserControl x:Class="AuroraDesignSuite.Views.GroundForcesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="{DynamicResource BgDarkBrush}" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Control & Filter Banner -->
        <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🪖 EJÉRCITOS TERRESTRES &amp; DESEMBARCO NAVAL" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                    <Border Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource AccentAmberBrush}" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                        <TextBlock x:Name="TxtSummaryBanner" Text="0 Formaciones | 0 Tons Totales" Foreground="{DynamicResource AccentAmberBrush}" FontSize="11" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Buscar Formación:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtSearchFormation" Width="180" Height="24" TextChanged="TxtSearchFormation_TextChanged" ToolTip="Filtra las formaciones terrestres por nombre o ubicación."/>
                    <Button Content="🔄 Actualizar" Click="BtnRefresh_Click" Padding="10,2" Margin="8,0,0,0" Background="{DynamicResource CardHeaderBrush}" Foreground="{DynamicResource AccentCyanBrush}" BorderBrush="{DynamicResource AccentCyanBrush}" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main 2-Column Content Layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 0: Active Ground Formations Grid -->
            <Border Grid.Column="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="📋 BRIGADAS, REGIMIENTOS Y BATERÍAS STO" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>

                    <DataGrid Grid.Row="1" x:Name="DgFormations" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DgFormations_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nombre de la Formación" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Ubicación / Planeta" Binding="{Binding LocationName}" Width="150"/>
                            <DataGridTextColumn Header="Tropas" Binding="{Binding TotalUnits, StringFormat={}{0:N0}}" Width="70"/>
                            <DataGridTextColumn Header="Peso (Tons)" Binding="{Binding TotalSizeTons, StringFormat={}{0:N0} t}" Width="95"/>
                            <DataGridTextColumn Header="Req. Hangar H-Tropas" Binding="{Binding RequiredTroopTransportHS, StringFormat={}{0:N0} HS}" Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Column 1: Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- Column 2: Formation Details & Transport Calculator -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <!-- Top: Elements Breakdown -->
                <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" x:Name="TxtSelectedFormationHeader" Text="🔍 COMPOSICIÓN DE TROPAS &amp; ARMAMENTO" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>

                        <DataGrid Grid.Row="1" x:Name="DgElements" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Clase de Tropa" Binding="{Binding ClassName}" Width="*"/>
                                <DataGridTextColumn Header="Tipo Base" Binding="{Binding BaseTypeName}" Width="100"/>
                                <DataGridTextColumn Header="Unidades" Binding="{Binding Units, StringFormat={}{0:N0}}" Width="65"/>
                                <DataGridTextColumn Header="Peso Unit." Binding="{Binding UnitWeightDisplay}" Width="90"/>
                                <DataGridTextColumn Header="Peso Total" Binding="{Binding TotalWeightDisplay}" Width="95"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Bottom: Troop Transport & Invasion Calculator -->
                <Border Grid.Row="2" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource AccentAmberBrush}" BorderThickness="1.5" CornerRadius="6" Padding="10" Margin="0,4,0,0">
                    <StackPanel>
                        <TextBlock Text="🚀 CALCULADORA DE LOGÍSTICA DE TRANSPORTE Y DESEMBARCO" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>

                        <Grid Margin="0,2">
                            <TextBlock Text="Masa Total de la Formación:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblCalcTotalTons" Text="0 t" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="Capacidad de Hangar de Tropas Requerida:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblCalcTroopHS" Text="0 HS" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="Capacidad Hangar Nave Transporte:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                            <ComboBox x:Name="CmbTransportCapacity" Width="180" Height="22" HorizontalAlignment="Right" SelectionChanged="CmbTransportCapacity_SelectionChanged" SelectedIndex="0">
                                <ComboBoxItem Content="5.000 t (Hangar 100 HS)"/>
                                <ComboBoxItem Content="10.000 t (Hangar 200 HS)"/>
                                <ComboBoxItem Content="25.000 t (Hangar 500 HS)"/>
                                <ComboBoxItem Content="50.000 t (Hangar 1.000 HS)"/>
                            </ComboBox>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="Naves de Transporte Requeridas:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblCalcShipsNeeded" Text="0 Naves" Foreground="{DynamicResource AccentGreenBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>

                        <Border Background="{DynamicResource BgDarkBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,8,0,0">
                            <TextBlock x:Name="TxtTacticalNote" Text="💡 Selecciona una formación en la lista para calcular el transporte marítimo/espacial exacto requerido para desembarcos u operaciones defensivas." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10.5" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
