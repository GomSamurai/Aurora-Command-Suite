<UserControl x:Class="AuroraDesignSuite.Views.ImperialChroniclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="{DynamicResource BgDarkBrush}" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Controls -->
        <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📜 HISTORIA E HISTORIAL DE EVENTOS IMPERIALES (ANALES DEL IMPERIO)" Foreground="{DynamicResource AccentGoldBrush}" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                    <Border Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource AccentGoldBrush}" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                        <TextBlock x:Name="TxtEventSummary" Text="0 Eventos Registrados" Foreground="{DynamicResource AccentGoldBrush}" FontSize="11" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Filtrar Anales:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtSearchChronicle" Width="200" Height="24" TextChanged="TxtSearchChronicle_TextChanged" ToolTip="Busca eventos por texto o categoría (e.g. combate, investigación, descubrimiento)."/>
                    <Button Content="🔄 Actualizar Anales" Click="BtnRefresh_Click" Padding="10,2" Margin="8,0,0,0" Background="{DynamicResource CardHeaderBrush}" Foreground="{DynamicResource AccentCyanBrush}" BorderBrush="{DynamicResource AccentCyanBrush}" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Events DataGrid -->
        <Border Grid.Row="1" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8">
            <DataGrid x:Name="DgEvents" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Categoría / Tipo Evento" Binding="{Binding CategoryIcon}" Width="180"/>
                    <DataGridTextColumn Header="Fecha Imperial / Stardate" Binding="{Binding FormattedDateDisplay}" Width="160"/>
                    <DataGridTextColumn Header="Detalle del Evento Histórico" Binding="{Binding MessageText}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
