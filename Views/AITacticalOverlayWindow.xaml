<Window x:Class="AuroraDesignSuite.Views.AITacticalOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aurora AI HUD Overlay" 
        Height="150" Width="420"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True" 
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    
    <Border x:Name="MainBorder" Background="#F00B0E14" BorderBrush="#00F0FF" BorderThickness="2" CornerRadius="8" Padding="12">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="4" Opacity="0.8"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header Row: Category Badge, Title & Close Button -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="BadgeBorder" Background="#2200F0FF" BorderBrush="#00F0FF" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                        <TextBlock x:Name="TxtCategoryBadge" Text="💡 CONSEJO TÁCTICO IA" Foreground="#00F0FF" FontWeight="Bold" FontSize="11"/>
                    </Border>
                </StackPanel>

                <TextBlock x:Name="TxtHeaderTime" Grid.Column="1" Text="AHORA" Foreground="#8B949E" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>

                <Button Grid.Column="2" Content="✕" Click="BtnClose_Click" Width="22" Height="22" Background="Transparent" Foreground="#8B949E" BorderThickness="0" FontWeight="Bold" FontSize="12" Cursor="Hand" ToolTip="Cerrar notificación"/>
            </Grid>

            <!-- Body Row: Message Text -->
            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TxtIcon" Grid.Column="0" Text="💡" FontSize="26" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <TextBlock x:Name="TxtMessage" Grid.Column="1" Text="Mensaje de prueba táctico de la IA en tiempo real." Foreground="White" FontSize="11.5" TextWrapping="Wrap" VerticalAlignment="Center" LineHeight="16"/>
            </Grid>

            <!-- Footer Row: Visual Progress Bar Countdown -->
            <Grid Grid.Row="2">
                <ProgressBar x:Name="PrgCountdown" Height="3" Background="#1C243C" Foreground="#00F0FF" BorderThickness="0" Value="100"/>
            </Grid>
        </Grid>
    </Border>
</Window>
