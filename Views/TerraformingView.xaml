<UserControl x:Class="AuroraDesignSuite.Views.TerraformingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="{DynamicResource BgDarkBrush}" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header & Colony Filter Banner -->
        <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🌱 SIMULADOR Y PLANIFICADOR DE TERRAFORMACIÓN IMPERIAL" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                    <Border Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource AccentAmberBrush}" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                        <TextBlock x:Name="TxtTerraformSummary" Text="0 Mundos bajo Monitoreo" Foreground="{DynamicResource AccentAmberBrush}" FontSize="11" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Buscar Colonia:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtSearchWorld" Width="180" Height="24" TextChanged="TxtSearchWorld_TextChanged" ToolTip="Filtra las colonias por nombre."/>
                    <Button Content="🔄 Actualizar" Click="BtnRefresh_Click" Padding="10,2" Margin="8,0,0,0" Background="{DynamicResource CardHeaderBrush}" Foreground="{DynamicResource AccentCyanBrush}" BorderBrush="{DynamicResource AccentCyanBrush}" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main 2-Column Content Layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 0: Imperial Planetary Systems & Colony Cost -->
            <Border Grid.Column="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="🪐 MUNDOS Y PLANETAS DE LA CORONA" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>

                    <DataGrid Grid.Row="1" x:Name="DgWorlds" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DgWorlds_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mundo / Colonia" Binding="{Binding PopName}" Width="*"/>
                            <DataGridTextColumn Header="Colony Cost (CC)" Binding="{Binding ColonyCostDisplay}" Width="150"/>
                            <DataGridTextColumn Header="Temperatura Superficial" Binding="{Binding TempDisplay}" Width="150"/>
                            <DataGridTextColumn Header="Presión" Binding="{Binding PressureDisplay}" Width="110"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Column 1: Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- Column 2: Selected World Atmosphere & Interactive Terraforming Calculator -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <!-- Row 0: Climate Summary Card -->
                <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,4">
                    <StackPanel>
                        <TextBlock x:Name="TxtWorldHeader" Text="🌍 ANÁLISIS ATMOSFÉRICO DE PLANETA" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>

                        <Grid Margin="0,2">
                            <TextBlock Text="Costo de Colonización (CC):" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblColonyCost" Text="0.0" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="Gravedad Superficial:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblGravity" Text="1.00 G" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,2">
                            <TextBlock Text="Cobertura de Agua / Hidrosfera:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblHydro" Text="70%" Foreground="{DynamicResource AccentGreenBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Row 1: Atmospheric Gases Table -->
                <Border Grid.Row="1" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,4,0,4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="☁️ COMPOSICIÓN QUÍMICA DE LA ATMÓSFERA" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>

                        <DataGrid Grid.Row="1" x:Name="DgGases" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DgGases_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Gas" Binding="{Binding GasName}" Width="*"/>
                                <DataGridTextColumn Header="Símbolo" Binding="{Binding Symbol}" Width="70"/>
                                <DataGridTextColumn Header="Presión Parcial" Binding="{Binding GasAtm, StringFormat={}{0:N4} atm}" Width="110"/>
                                <DataGridTextColumn Header="Propiedad Térmica" Binding="{Binding GasTypeDisplay}" Width="160"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Row 2 & 3: Interactive Terraforming Installation & Time Simulator -->
                <Border Grid.Row="3" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,4,0,0">
                    <StackPanel>
                        <TextBlock Text="⚙️ SIMULADOR INTERACTIVO DE INSTALACIONES Y TIEMPO ESTIMADO" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,8"/>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Instalaciones de Terraformación:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" x:Name="SliderInstallations" Minimum="1" Maximum="500" Value="20" TickFrequency="10" IsSnapToTickEnabled="True" ValueChanged="SliderInstallations_ValueChanged" Margin="10,0"/>
                            <TextBlock Grid.Column="2" x:Name="TxtInstallationsCount" Text="20 Instalaciones" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Width="120" TextAlignment="Right"/>
                        </Grid>

                        <Border Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource AccentCyanBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock x:Name="TxtSimRate" Text="⚡ Tasa de Inyección: 0,0200 atm / año" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="12"/>
                                <TextBlock x:Name="TxtSimTime" Text="⏳ Tiempo Estimado para Lograr Atmósfera Respirable: ~12.5 Años Imperiales" Foreground="{DynamicResource AccentGreenBrush}" FontWeight="Bold" FontSize="12" Margin="0,4,0,0"/>
                                <TextBlock Text="💡 Tip Imperial: Modifica el slider para calcular al instante el impacto de construir más instalaciones o desplegar Naves Terraformadoras orbitales." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10" FontStyle="Italic" Margin="0,6,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
