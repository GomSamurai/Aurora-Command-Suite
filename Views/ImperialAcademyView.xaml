<UserControl x:Class="AuroraDesignSuite.Views.ImperialAcademyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" MinWidth="260"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Column Splitter -->
        <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent" Cursor="SizeWE" ShowsPreview="True">
            <GridSplitter.Template>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="{DynamicResource BorderBrush}" Width="2" HorizontalAlignment="Center"/>
                </ControlTemplate>
            </GridSplitter.Template>
        </GridSplitter>

        <!-- Left Panel: Lesson Selector & Interactive Codex Search -->
        <Grid Grid.Column="0" Margin="0,0,4,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Card 1: Header Banner -->
            <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource AccentAmberBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="🎓 ACADEMIA IMPERIAL 4X" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
                    <TextBlock Text="Centro de Tutoría Táctica, Lecciones Interactivas y Diccionario de Conceptos." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="10.5" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Card 2: Interactive Search Bar -->
            <Border Grid.Row="1" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="🔍 BUSCADOR DE CONCEPTOS &amp; SIGLAS" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="11" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtSearchCodex" TextChanged="TxtSearchCodex_TextChanged" Padding="6,4" Background="{DynamicResource CardBgBrush}" Foreground="{DynamicResource AccentCyanBrush}" BorderBrush="{DynamicResource BorderBrush}" ToolTip="Escribe cualquier sigla o concepto (ej. HS, TCS, DCR, Mass Driver, Retool, Sorium...)"/>
                </StackPanel>
            </Border>

            <!-- Card 3: Lessons & Terms List -->
            <Border Grid.Row="2" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="10">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="📚 LECCIONES Y ARTÍCULOS TÁCTICOS" Foreground="{DynamicResource AccentGreenBrush}" FontWeight="Bold" FontSize="11" Margin="0,0,0,8"/>
                    <ListBox x:Name="LstCodexItems" SelectionChanged="LstCodexItems_SelectionChanged" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimaryBrush}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8,6" Margin="0,2" Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="11"/>
                                        <TextBlock Text="{Binding Category}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9.5"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Right Panel: Detailed Lesson / Concept Reader -->
        <Border Grid.Column="2" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="16" Margin="4,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="PnlArticleContent">
                    <TextBlock x:Name="LblArticleTitle" Text="Bienvenido a la Academia Imperial Aurora 4X" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                    <Border Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource AccentCyanBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,14">
                        <TextBlock x:Name="LblArticleCategory" Text="🎓 Módulo de Tutoría y Guía Táctica en Vivo" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="11"/>
                    </Border>
                    
                    <TextBlock x:Name="LblArticleBody" Text="Selecciona una lección o busca cualquier término a la izquierda para ver la explicación detallada, fórmulas matemáticas y consejos prácticos en juego." Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12" TextWrapping="Wrap" LineHeight="20"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
