<UserControl x:Class="AuroraDesignSuite.Views.XenoIntelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="{DynamicResource BgDarkBrush}" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Control Banner -->
        <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="👽 CENTRO DE INTELIGENCIA ALIENÍGENA Y XENODIPLOMACIA" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                    <Border Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource AccentAmberBrush}" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                        <TextBlock x:Name="TxtXenoSummary" Text="0 Razas Alienígenas Contactadas" Foreground="{DynamicResource AccentAmberBrush}" FontSize="11" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Buscar Alienígenas:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtSearchXeno" Width="180" Height="24" TextChanged="TxtSearchXeno_TextChanged" ToolTip="Filtra las razas o naves por nombre o abreviatura."/>
                    <Button Content="🔄 Actualizar" Click="BtnRefresh_Click" Padding="10,2" Margin="8,0,0,0" Background="{DynamicResource CardHeaderBrush}" Foreground="{DynamicResource AccentCyanBrush}" BorderBrush="{DynamicResource AccentCyanBrush}" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main 2-Column Content Layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 0: Detected Alien Races & Intelligence -->
            <Border Grid.Column="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="🛸 ESPECIES &amp; CIVILIZACIONES NPR DETECTADAS" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>

                    <DataGrid Grid.Row="1" x:Name="DgAlienRaces" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DgAlienRaces_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Designación Alienígena" Binding="{Binding AlienRaceName}" Width="*"/>
                            <DataGridTextColumn Header="Código" Binding="{Binding Abbrev}" Width="65"/>
                            <DataGridTextColumn Header="Estado de Xenodiplomacia" Binding="{Binding CommStatusDisplay}" Width="200"/>
                            <DataGridTextColumn Header="Nivel de Amenaza" Binding="{Binding ThreatLevelDisplay}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Column 1: Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- Column 2: Selected Race Detail & Observed Classes -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <!-- Top: Diplomatic Dossier & Protocol -->
                <Border Grid.Row="0" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,4">
                    <StackPanel>
                        <TextBlock x:Name="TxtXenoHeader" Text="👽 ANÁLISIS DE INTELIGENCIA Y DIRECTIVA DE CONTACTO" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>

                        <Grid Margin="0,3">
                            <TextBlock Text="Puntos Diplomáticos Académicos:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblXenoPoints" Text="0,0" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,3">
                            <TextBlock Text="Tratados &amp; Acuerdos Vigentes:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblXenoTreaties" Text="Sin Tratados" Foreground="{DynamicResource AccentCyanBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,3">
                            <TextBlock Text="Daño Causado a las Fuerzas Imperiales:" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="LblXenoDamage" Text="0,0 t Hundidas" Foreground="{DynamicResource AccentRedBrush}" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>

                        <Border Background="{DynamicResource CardHeaderBrush}" BorderBrush="{DynamicResource AccentAmberBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                            <TextBlock x:Name="TxtProtocolText" Text="⚠️ Protocolo Alerta Verde: No se detectan hostilidades activas. Se mantiene patrullaje preventivo de sensores pasivos y monitoreo en los puntos de salto." Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Bottom: Observed Alien Warship Classes -->
                <Border Grid.Row="2" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,4,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="🚀 CLASES DE NAVES XENO OBSERVADAS (ANÁLISIS SENSORES)" Foreground="{DynamicResource AccentAmberBrush}" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>

                        <DataGrid Grid.Row="1" x:Name="DgAlienClasses" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Clase Xeno" Binding="{Binding ClassName}" Width="*"/>
                                <DataGridTextColumn Header="Velocidad Máx." Binding="{Binding SpeedDisplay}" Width="95"/>
                                <DataGridTextColumn Header="Firma Térmica" Binding="{Binding ThermalDisplay}" Width="95"/>
                                <DataGridTextColumn Header="Blindaje / Escudos" Binding="{Binding DefenseSummary}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
